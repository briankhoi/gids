# gids
Git Identity Swap

`gids` is a CLI tool to manage multiple Git identities (e.g., work, personal, school) and automatically switch between them based on the current directory. It manages local `.git/config` settings and SSH identities to prevent accidental commits with the wrong Git profile.
